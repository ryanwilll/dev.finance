{
  "scripts": {
    "start": "live-server --port=8080",
    "cy:open": "cypress open",
    "test": "concurrently \"npm run start\" \"npm run cy:open\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "cypress": "^13.15.2",
    "live-server": "^1.2.2"
  }
}