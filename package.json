{
  "scripts": {
    "start": "live-server --port=8080",
    "test": "cypress run",
    "testLocal": "concurrently --kill-others \"npm run start\" \"npm run cy:run\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "cypress": "^13.15.2",
    "live-server": "^1.2.2"
  }
}